# 🔐 ระบบแอดมิน Versecanvas - คู่มือฉบับสมบูรณ์

## 📋 สารบัญ

1. [ภาพรวม](#ภาพรวม)
2. [การเข้าถึงระบบแอดมิน](#การเข้าถึงระบบแอดมิน)
3. [ฟีเจอร์ทั้งหมด](#ฟีเจอร์ทั้งหมด)
4. [การสร้างบัญชีแอดมิน](#การสร้างบัญชีแอดมิน)
5. [โครงสร้างไฟล์](#โครงสร้างไฟล์)
6. [การใช้งาน](#การใช้งาน)

---

## 🎯 ภาพรวม

ระบบแอดมิน Versecanvas เป็นระบบจัดการเว็บไซต์แบบครบวงจร ที่ช่วยให้ผู้ดูแลระบบสามารถ:
- จัดการผู้ใช้ทั้งหมด
- ควบคุมเนื้อหา (นิยาย, ผลงานศิลปะ)
- ตรวจสอบธุรกรรมและรายได้
- ดูสถิติและวิเคราะห์ข้อมูล
- จัดการรายงานจากผู้ใช้

---

## 🔑 การเข้าถึงระบบแอดมิน

### URL
```
https://your-domain.com/admin
```

### สิทธิ์การเข้าถึง
- เฉพาะผู้ใช้ที่มี `role: "admin"` ใน Firestore เท่านั้น
- ผู้ใช้ทั่วไปจะถูกปฏิเสธการเข้าถึงอัตโนมัติ
- ต้อง login ก่อนเข้าใช้งาน

---

## 🎨 ฟีเจอร์ทั้งหมด

### 1. **Admin Dashboard** (`/admin`)
**หน้าแรกของระบบแอดมิน**

**ฟีเจอร์:**
- ✅ สถิติภาพรวม (ผู้ใช้, นิยาย, ผลงาน, รายได้)
- ✅ กราฟการเติบโต
- ✅ กิจกรรมล่าสุด
- ✅ Quick Actions (ลัดไปหน้าต่างๆ)

**สถิติที่แสดง:**
- จำนวนผู้ใช้ทั้งหมด
- จำนวนนิยายทั้งหมด
- จำนวนผลงานศิลปะ
- รายได้ทั้งหมด
- ผู้ใช้ใหม่วันนี้
- ผู้ใช้ที่ Active
- สินค้าทั้งหมด
- ธุรกรรมทั้งหมด

---

### 2. **User Management** (`/admin/users`)
**จัดการผู้ใช้ทั้งหมดในระบบ**

**ฟีเจอร์:**
- ✅ ดูรายชื่อผู้ใช้ทั้งหมด
- ✅ ค้นหาผู้ใช้ (ชื่อ, อีเมล, ID)
- ✅ แก้ไขข้อมูลผู้ใช้
- ✅ เปลี่ยน Role (user, moderator, admin)
- ✅ แบน/ปลดแบนผู้ใช้
- ✅ ลบผู้ใช้
- ✅ สถิติแยกตาม Role

**การแก้ไขผู้ใช้:**
```
- ชื่อผู้ใช้ (displayName)
- Role (user/moderator/admin)
- สถานะแบน (banned: true/false)
```

**Role ที่มี:**
- `user` - ผู้ใช้ทั่วไป
- `moderator` - ผู้ช่วยดูแล (อนาคต)
- `admin` - ผู้ดูแลระบบ

---

### 3. **Content Management** (`/admin/content`)
**จัดการเนื้อหาทั้งหมด**

**ฟีเจอร์:**
- ✅ แท็บนิยาย / ผลงานศิลปะ
- ✅ ค้นหาเนื้อหา
- ✅ ดูรายละเอียดเนื้อหา
- ✅ ซ่อน/แสดงเนื้อหา
- ✅ ลบเนื้อหา
- ✅ ลบนิยายพร้อมตอนทั้งหมด

**การจัดการนิยาย:**
- ดูรายการนิยายทั้งหมด
- ดูผู้สร้าง
- ซ่อนนิยายที่ไม่เหมาะสม
- ลบนิยายและตอนทั้งหมด

**การจัดการผลงานศิลปะ:**
- ดูรายการผลงานทั้งหมด
- ดูผู้สร้าง
- ซ่อนผลงานที่ไม่เหมาะสม
- ลบผลงาน

---

### 4. **Transaction Management** (`/admin/transactions`)
**จัดการธุรกรรมทั้งหมด**

**ฟีเจอร์:**
- ✅ ดูธุรกรรมทั้งหมด
- ✅ ค้นหาธุรกรรม
- ✅ กรองตามประเภท
- ✅ Export เป็น CSV
- ✅ สถิติรายได้

**ประเภทธุรกรรม:**
- `purchase` - ซื้อ (สีน้ำเงิน)
- `topup` - เติมเงิน (สีเขียว)
- `withdrawal` - ถอนเงิน (สีส้ม)
- `refund` - คืนเงิน (สีแดง)
- `reward` - รางวัล (สีม่วง)
- `transfer` - โอน (สีฟ้า)

**สถิติ:**
- รายได้ทั้งหมด
- การซื้อทั้งหมด
- การถอนเงินทั้งหมด
- การคืนเงินทั้งหมด

---

### 5. **Reports** (`/admin/reports`)
**ตรวจสอบรายงานจากผู้ใช้**

**ฟีเจอร์:**
- ✅ ดูรายงานทั้งหมด
- ✅ กรองตามสถานะ (รอตรวจสอบ, อนุมัติ, ปฏิเสธ)
- ✅ อนุมัติและลบเนื้อหา
- ✅ ปฏิเสธรายงาน
- ✅ ดูเนื้อหาที่ถูกรายงาน

**สถานะรายงาน:**
- `pending` - รอตรวจสอบ (สีเหลือง)
- `approved` - อนุมัติ (สีเขียว)
- `rejected` - ปฏิเสธ (สีแดง)

---

### 6. **Analytics** (`/admin/analytics`)
**สถิติและการวิเคราะห์ข้อมูล**

**ฟีเจอร์:**
- ✅ กราฟการเติบโตของผู้ใช้
- ✅ กราฟการเติบโตของเนื้อหา
- ✅ กราฟการเติบโตของรายได้
- ✅ Key Metrics (User Growth Rate, Engagement Rate, Revenue Growth)

**กราฟที่มี:**
1. **User Growth** - จำนวนผู้ใช้แต่ละเดือน
2. **Content Growth** - จำนวนนิยายและผลงานแต่ละเดือน
3. **Revenue Growth** - รายได้แต่ละเดือน

---

## 👤 การสร้างบัญชีแอดมิน

### วิธีที่ 1: ผ่าน Firebase Console (แนะนำ)

1. เข้า **Firebase Console**: https://console.firebase.google.com
2. เลือกโปรเจค **Versecanvas**
3. ไปที่ **Authentication** → **Users**
4. คลิก **Add user** หรือเลือก user ที่มีอยู่
5. ไปที่ **Firestore Database**
6. เข้า collection `users`
7. เลือก document ของ user (ใช้ UID)
8. แก้ไขหรือเพิ่ม field:
   ```
   role: "admin"
   ```
9. บันทึก

### วิธีที่ 2: ผ่าน Firestore Console โดยตรง

1. เข้า **Firestore Database**
2. Collection: `users`
3. Document ID: `[UID ของผู้ใช้]`
4. เพิ่ม/แก้ไข fields:
   ```json
   {
     "email": "admin@versecanvas.com",
     "displayName": "Admin",
     "role": "admin",
     "createdAt": [timestamp],
     "updatedAt": [timestamp]
   }
   ```

### วิธีที่ 3: ใช้ Firebase Admin SDK (ต้องมี Service Account Key)

```javascript
const admin = require('firebase-admin');
const serviceAccount = require('./serviceAccountKey.json');

admin.initializeApp({
  credential: admin.credential.cert(serviceAccount)
});

const db = admin.firestore();

async function createAdmin(uid) {
  await db.collection('users').doc(uid).update({
    role: 'admin'
  });
  console.log('Admin created successfully!');
}

// ใช้งาน
createAdmin('USER_UID_HERE');
```

---

## 📁 โครงสร้างไฟล์

```
src/
├── components/
│   ├── AdminRoute.jsx          # ป้องกันหน้าแอดมิน
│   ├── AdminLayout.jsx         # Layout พร้อม sidebar
│   └── Navbar.jsx              # Navbar หลัก (responsive)
│
├── pages/
│   └── admin/
│       ├── Dashboard.jsx       # หน้าแรกแอดมิน
│       ├── UserManagement.jsx  # จัดการผู้ใช้
│       ├── ContentManagement.jsx # จัดการเนื้อหา
│       ├── TransactionManagement.jsx # จัดการธุรกรรม
│       ├── Reports.jsx         # รายงาน
│       └── Analytics.jsx       # วิเคราะห์ข้อมูล
│
├── contexts/
│   └── AuthContext.jsx         # จัดการ authentication + role
│
└── App.jsx                     # Routes หลัก
```

---

## 🚀 การใช้งาน

### 1. เข้าสู่ระบบ
```
1. ไปที่ /login
2. Login ด้วยบัญชีที่มี role: "admin"
3. จะถูก redirect ไปหน้าแรก
```

### 2. เข้าระบบแอดมิน
```
1. ไปที่ /admin
2. ถ้าไม่ใช่ admin จะถูกปฏิเสธ
3. ถ้าเป็น admin จะเห็น Dashboard
```

### 3. Navigation
```
- ใช้ Sidebar ด้านซ้าย (Desktop)
- ใช้ Hamburger menu (Mobile)
- คลิก "กลับหน้าแรก" เพื่อกลับเว็บหลัก
```

---

## 🎨 UI/UX

### สีและธีม
- **Background**: `#0a0a0a` (ดำ)
- **Card**: `#1a1a1a` (เทาเข้ม)
- **Border**: `#2a2a2a` (เทา)
- **Primary**: `purple-600` (ม่วง)
- **Text**: `white`, `gray-300`, `gray-400`

### Responsive
- ✅ Desktop: Sidebar แบบถาวร
- ✅ Mobile: Hamburger menu
- ✅ Tablet: ปรับ layout อัตโนมัติ

---

## 🔒 ความปลอดภัย

### Role-Based Access Control
```javascript
// AdminRoute.jsx
if (userProfile.role !== 'admin') {
  return <AccessDenied />;
}
```

### ตรวจสอบสิทธิ์
- ทุกหน้าแอดมินผ่าน `<AdminRoute>`
- ตรวจสอบ `userProfile.role` จาก Firestore
- ไม่ใช่ admin = ไม่สามารถเข้าถึง

---

## 📊 Database Schema

### Collection: `users`
```javascript
{
  uid: string,
  email: string,
  displayName: string,
  photoURL: string,
  bio: string,
  role: "user" | "moderator" | "admin",  // สำคัญ!
  banned: boolean,
  createdAt: timestamp,
  updatedAt: timestamp
}
```

### Collection: `stories`
```javascript
{
  id: string,
  title: string,
  description: string,
  coverImage: string,
  authorId: string,
  genre: string,
  hidden: boolean,  // ซ่อนโดยแอดมิน
  createdAt: timestamp
}
```

### Collection: `artworks`
```javascript
{
  id: string,
  title: string,
  description: string,
  imageUrl: string,
  userId: string,
  hidden: boolean,  // ซ่อนโดยแอดมิน
  createdAt: timestamp
}
```

### Collection: `transactions`
```javascript
{
  id: string,
  userId: string,
  type: "purchase" | "topup" | "withdrawal" | "refund" | "reward" | "transfer",
  amount: number,
  description: string,
  createdAt: timestamp
}
```

---

## 🛠️ การพัฒนาต่อ

### เพิ่มฟีเจอร์ใหม่

1. **สร้างหน้าใหม่**
   ```javascript
   // src/pages/admin/NewFeature.jsx
   export default function NewFeature() {
     return <div>New Feature</div>;
   }
   ```

2. **เพิ่ม Route**
   ```javascript
   // App.jsx
   import NewFeature from './pages/admin/NewFeature';
   
   <Route path="/admin/new-feature" element={
     <AdminRoute>
       <AdminLayout>
         <NewFeature />
       </AdminLayout>
     </AdminRoute>
   } />
   ```

3. **เพิ่มใน Sidebar**
   ```javascript
   // AdminLayout.jsx
   const menuItems = [
     // ...existing items
     { path: '/admin/new-feature', icon: Icon, label: 'New Feature' }
   ];
   ```

---

## 📝 TODO / ฟีเจอร์ในอนาคต

- [ ] Settings page (ตั้งค่าเว็บไซต์)
- [ ] Email notifications ถึงผู้ใช้
- [ ] Bulk actions (ลบหลายรายการพร้อมกัน)
- [ ] Advanced analytics (กราฟเพิ่มเติม)
- [ ] Export reports (PDF)
- [ ] Scheduled tasks (ส่งอีเมลอัตโนมัติ)
- [ ] Moderator role (สิทธิ์ระดับกลาง)
- [ ] Activity logs (บันทึกการกระทำของแอดมิน)

---

## 🐛 การแก้ไขปัญหา

### ปัญหา: ไม่สามารถเข้าหน้าแอดมินได้
**แก้ไข:**
1. ตรวจสอบว่า login แล้วหรือยัง
2. ตรวจสอบ role ใน Firestore: `users/{uid}/role` ต้องเป็น `"admin"`
3. ลอง logout และ login ใหม่

### ปัญหา: Sidebar ไม่แสดงบนมือถือ
**แก้ไข:**
1. คลิกปุ่ม hamburger (3 ขีด) ที่มุมซ้ายบน
2. ตรวจสอบ responsive CSS

### ปัญหา: ข้อมูลไม่แสดง
**แก้ไข:**
1. ตรวจสอบ Firestore Rules
2. ตรวจสอบ Console (F12) หา error
3. ตรวจสอบว่ามี collection และ documents หรือไม่

---

## 📞 ติดต่อ/สนับสนุน

หากมีปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบเอกสารนี้อีกครั้ง
2. ดู Console (F12) หา error messages
3. ตรวจสอบ Firebase Console

---

## 🎉 สรุป

ระบบแอดมิน Versecanvas พร้อมใช้งานแล้ว!

**ฟีเจอร์ที่มี:**
- ✅ Dashboard พร้อมสถิติ
- ✅ User Management
- ✅ Content Management
- ✅ Transaction Management
- ✅ Reports
- ✅ Analytics
- ✅ Role-Based Access Control
- ✅ Responsive Design

**การใช้งาน:**
1. สร้างบัญชีแอดมิน (ตั้ง role: "admin" ใน Firestore)
2. Login
3. ไปที่ `/admin`
4. เริ่มจัดการเว็บไซต์!

---

*เอกสารนี้สร้างเมื่อ: 2026-01-13*  
*เวอร์ชัน: 1.0.0*
