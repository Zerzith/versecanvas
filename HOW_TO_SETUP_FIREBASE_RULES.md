# 🔒 คู่มือตั้งค่า Firebase Rules

## 📋 ภาพรวม

Firebase มี **2 ประเภท Rules** ที่ต้องตั้งค่า:

1. **Firestore Rules** - ควบคุมการเข้าถึง Firestore Database
2. **Realtime Database Rules** - ควบคุมการเข้าถึง Realtime Database

---

## 🔥 ส่วนที่ 1: ตั้งค่า Firestore Rules

### ขั้นตอนที่ 1: เข้าสู่ Firebase Console

1. **เปิดเบราว์เซอร์** ไปที่: https://console.firebase.google.com/
2. **Login** ด้วย Google Account
3. **เลือกโปรเจค** `versecanvas-a9b76`

---

### ขั้นตอนที่ 2: เข้าสู่หน้า Firestore Rules

1. **คลิกเมนู** "Firestore Database" (ด้านซ้าย)
2. **คลิกแท็บ** "Rules" (ด้านบน)
3. จะเห็นหน้าจอแก้ไข Rules

---

### ขั้นตอนที่ 3: เปิดไฟล์ Rules ในโปรเจค

1. **เปิดโปรเจค** ที่แตก zip แล้ว
2. **หาไฟล์** `firestore.rules`
3. **เปิดด้วย** Text Editor (Notepad, VS Code, หรืออะไรก็ได้)
4. **คัดลอก** โค้ดทั้งหมดในไฟล์

---

### ขั้นตอนที่ 4: วาง Rules ใน Firebase Console

1. **กลับไปที่** Firebase Console (หน้า Firestore Rules)
2. **ลบโค้ดเดิม** ทั้งหมด (Ctrl+A แล้ว Delete)
3. **วางโค้ด** ที่คัดลอกมา (Ctrl+V)
4. **ตรวจสอบ** ว่าโค้ดถูกต้อง (ไม่มีสีแดง)

---

### ขั้นตอนที่ 5: Publish Rules

1. **คลิกปุ่ม** "Publish" (สีน้ำเงิน ขวาบน)
2. **รอสักครู่** จะเห็นข้อความ "Rules published successfully"
3. **เสร็จสิ้น!** Firestore Rules ตั้งค่าเรียบร้อยแล้ว

---

## 📱 ส่วนที่ 2: ตั้งค่า Realtime Database Rules

### ขั้นตอนที่ 1: เข้าสู่หน้า Realtime Database

1. **อยู่ใน Firebase Console** แล้ว
2. **คลิกเมนู** "Realtime Database" (ด้านซ้าย)
3. **คลิกแท็บ** "Rules" (ด้านบน)
4. จะเห็นหน้าจอแก้ไข Rules (รูปแบบ JSON)

---

### ขั้นตอนที่ 2: เปิดไฟล์ Rules ในโปรเจค

1. **เปิดโปรเจค** ที่แตก zip แล้ว
2. **หาไฟล์** `database.rules.json`
3. **เปิดด้วย** Text Editor
4. **คัดลอก** โค้ดทั้งหมดในไฟล์

---

### ขั้นตอนที่ 3: วาง Rules ใน Firebase Console

1. **กลับไปที่** Firebase Console (หน้า Realtime Database Rules)
2. **ลบโค้ดเดิม** ทั้งหมด
3. **วางโค้ด** ที่คัดลอกมา
4. **ตรวจสอบ** ว่าโค้ดถูกต้อง (ไม่มีสีแดง)

---

### ขั้นตอนที่ 4: Publish Rules

1. **คลิกปุ่ม** "Publish" (สีน้ำเงิน ขวาบน)
2. **รอสักครู่** จะเห็นข้อความ "Rules published successfully"
3. **เสร็จสิ้น!** Realtime Database Rules ตั้งค่าเรียบร้อยแล้ว

---

## ✅ ตรวจสอบว่าตั้งค่าสำเร็จ

### ตรวจสอบ Firestore Rules:

1. ไปที่ **Firestore Database** > **Rules**
2. ต้องเห็นโค้ดที่เริ่มต้นด้วย:
```
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    ...
  }
}
```

---

### ตรวจสอบ Realtime Database Rules:

1. ไปที่ **Realtime Database** > **Rules**
2. ต้องเห็นโค้ดที่เริ่มต้นด้วย:
```json
{
  "rules": {
    "messages": {
      ...
    }
  }
}
```

---

## 📋 สรุปสั้นๆ

### Firestore Rules:
1. Firestore Database > Rules
2. คัดลอกจาก `firestore.rules`
3. วางแทนที่โค้ดเดิม
4. คลิก Publish

### Realtime Database Rules:
1. Realtime Database > Rules
2. คัดลอกจาก `database.rules.json`
3. วางแทนที่โค้ดเดิม
4. คลิก Publish

---

## 🎯 ทำไมต้องตั้งค่า Rules?

### Firestore Rules ควบคุม:
- ✅ ใครอ่านนิยายได้บ้าง
- ✅ ใครแก้ไขนิยายได้บ้าง
- ✅ ใครเห็นคำสั่งซื้อได้บ้าง
- ✅ ใครเห็นธุรกรรมได้บ้าง
- ✅ ป้องกันคนอื่นแก้ไขข้อมูลของเรา

### Realtime Database Rules ควบคุม:
- ✅ ใครส่งข้อความได้บ้าง
- ✅ ใครดูข้อความได้บ้าง
- ✅ ใครกดไลค์ได้บ้าง
- ✅ ใครดูยอดรับชมได้บ้าง

---

## 🚨 ข้อควรระวัง

### ❌ ห้ามทำ:

1. **ห้ามใช้ Rules แบบ Test Mode**
```
// ❌ อันตราย! ทุกคนเข้าถึงได้หมด
allow read, write: if true;
```

2. **ห้ามลบ Rules ทั้งหมด**
- จะทำให้ไม่มีความปลอดภัย

3. **ห้ามแก้ไข Rules เอง** (ถ้าไม่เข้าใจ)
- ใช้ Rules ที่ให้ไปเลย

---

## ✅ Rules ที่ให้มามีอะไรบ้าง?

### Firestore Rules ครอบคลุม:

1. **Users** - ผู้ใช้แก้ไขโปรไฟล์ตัวเองได้เท่านั้น
2. **Stories** - ทุกคนอ่านได้, แต่แก้ไขได้เฉพาะเจ้าของ
3. **Chapters** - ทุกคนอ่านได้, แต่แก้ไขได้เฉพาะเจ้าของ
4. **Artworks** - ทุกคนดูได้, แต่แก้ไขได้เฉพาะเจ้าของ
5. **Products** - ทุกคนดูได้, แต่แก้ไขได้เฉพาะผู้ขาย
6. **Jobs** - ผู้ล็อกอินดูได้, แก้ไขได้เฉพาะผู้เกี่ยวข้อง
7. **Orders** - เห็นได้เฉพาะผู้ซื้อและผู้ขาย
8. **Transactions** - เห็นได้เฉพาะคนที่เกี่ยวข้อง
9. **Notifications** - เห็นได้เฉพาะเจ้าของ
10. **Bookmarks** - เห็นได้เฉพาะเจ้าของ

### Realtime Database Rules ครอบคลุม:

1. **Messages** - ผู้ล็อกอินอ่านและเขียนได้
2. **Conversations** - เห็นได้เฉพาะเจ้าของ
3. **Views** - ทุกคนดูได้, ผู้ล็อกอินเพิ่มได้
4. **Likes** - ทุกคนดูได้, ผู้ล็อกอินเพิ่มได้
5. **Follows** - ทุกคนดูได้, ผู้ล็อกอินเพิ่มได้

---

## 💡 เคล็ดลับ

### ถ้าตั้งค่าแล้วยังใช้งานไม่ได้:

1. **ตรวจสอบว่า Publish แล้ว** (ปุ่มต้องไม่สีน้ำเงิน)
2. **Refresh เว็บไซต์** (F5)
3. **ลอง Logout แล้ว Login ใหม่**
4. **ดู Console** (F12) ว่ามี error อะไร

### ถ้าเห็น Error "Permission denied":

1. **ตรวจสอบว่าล็อกอินแล้ว**
2. **ตรวจสอบว่า Rules ตั้งค่าถูกต้อง**
3. **ตรวจสอบว่า userId ตรงกัน**

---

## 🎊 เสร็จสิ้น!

หลังจากตั้งค่า Rules แล้ว:

1. ✅ ข้อมูลปลอดภัย
2. ✅ ผู้ใช้เข้าถึงได้เฉพาะข้อมูลของตัวเอง
3. ✅ ป้องกันคนอื่นแก้ไขข้อมูลของเรา
4. ✅ เว็บไซต์พร้อมใช้งาน

---

## 📞 ถ้ามีปัญหา

ส่งภาพหน้าจอมาได้เลย:
- หน้า Firestore Rules
- หน้า Realtime Database Rules
- Error message ใน Console (F12)

**ขอให้โชคดี! 🚀✨**
